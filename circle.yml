dependencies:
  override:
    - chmod +x bin/*
    - ./bin/clean.sh
    - ./bin/build.sh

test:
  override:
    - ./bin/test.sh

deployment:
  master:
    branch: [test, master]
    commands:
      - ./bin/deploy.sh $CIRCLE_BRANCH
